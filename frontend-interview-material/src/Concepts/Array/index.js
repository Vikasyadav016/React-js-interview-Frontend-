const ArrayConcepts = () => {

    //    Array is an ordered, indexed data structured to store miltiple value in a single variable/collection. 
    //    In javascript array is an specialized object which provide, dinimic sizing, zero-based indexing and 
    //    rich set of build in function to manipulate and iterate on arrays data 
    //    The example of creating an array is as below.

    const arrayOne = ['Rakesh', 'Ankur', 'rajesh', 'Arun', 'Anuj'];

    //      in above const is datatype which is showing that array is of cont datatype.
    //      arrayOne is array name
    //      and 'Rakesh','Ankur','rajesh','Arun','Anuj' these are elements of array arrayOne.

    //      as we know in js array is a specialized object 
    console.log(typeof arrayOne)
    //      by typeof chaecking it will show arrayOne is of type object
    //      Output will be Object

    // differences between array and Object

    //                  Array                                                                   Object
    // 1.) Array is an ordered list                                                         Key - value pair
    // 2.) Indexing key based                                                                   named key
    // 3.) 
    return (
        <>
            <h3>Array concepts</h3>
        </>
    )
}